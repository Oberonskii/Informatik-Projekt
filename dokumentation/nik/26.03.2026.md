# Dokumentation Nik - 26.03.2026

## Kontext
- Ziel: Vollstaendige Sprachumschaltung Deutsch/Englisch fuer die gesamte Website.
- Fokus heute: Restliche nicht uebersetzte Bereiche nachziehen (Tabs, Hausaufgaben-Placeholder, Flashcards komplett, Kalender komplett).

## Prompts (User)
1. "Füge eine vollständig funktionierende Einstellung in den Account-Einstellungen hinzu wo man von Deutsch auf Englisch wechseln kann und auch zurück. Die vollständige Website soll dann der entsprechenden Sprache angepasst werden"
2. "Teilweise sind immer noch Sachen auf Deutsch wenn man auf Englisch stellt wie: Die Namen der Tabs, im Hausaufgaben-Tab der Hintergrund im Feld zur Hausaufgabe einfügen, im Karteikarten-Tab alles und im Stundenplan die Tage Montag, Dienstag usw"
3. "Im kalender tab muss auch alles der sprache entsprechend angepasst werden"
4. "aktualisiere die heutige dokumentation von nik mit meinen prompts, deinen ausgaben und allem was du am code geändert hast und commit und push anschließend die änderungen"

## Antworten / Outputs (Assistant) - Kurzprotokoll
- i18n-Infrastruktur eingefuehrt und ueber mehrere Dateien ausgerollt.
- Fehlende harte DE-Strings in JS/HTML lokalisiert.
- Flashcards-Tab (HTML + JS) auf i18n umgestellt.
- Kalender-Tab (Template + JS-Logik) auf i18n umgestellt, inkl. Monatsnamen, Wochentagskuerzel, Empty-States und Recurrence-Texte.
- PHP-Syntaxpruefungen durchgefuehrt (`php -l`) ohne Fehler.

## Codeaenderungen im Detail

### Neu angelegt
- `includes/i18n.php`
  - Zentrale Uebersetzungsverwaltung (`t()`, `learnhub_get_locale()`, `learnhub_set_locale()`).
  - DE/EN Key-Sets fuer Navigation, Tabs, Modals, JS-Texte, Flashcards, Kalender etc.
- `auth/set_language.php`
  - Speichert gewaehlte Sprache in Session und leitet zurueck.

### Geaendert (Backend/Auth)
- `auth/login.php`
  - i18n eingebunden.
  - Sprache via Session/`lang` beruecksichtigt.
  - Titel/Fehlermeldungen lokalisiert.
- `auth/register.php`
  - i18n eingebunden.
  - Sprache via Session/`lang` beruecksichtigt.
  - Titel/Erfolgs- und Fehlermeldungen lokalisiert.

### Geaendert (Dashboard + JS)
- `current_dashboard.php`
  - i18n eingebunden, `lang`-Attribut dynamisch.
  - Sidebar-Namen auf `t('nav.*')` umgestellt.
  - Account-Modal inkl. Sprachumschalter integriert.
  - Zentrale JS-I18N-Struktur eingefuehrt/erweitert (`const I18N = {...}` + `i18nFormat`).
  - Stundenplan-Tage (`TT_DAY_NAMES`) via i18n statt hardcoded Deutsch.
  - Hausaufgaben-Strings lokalisiert (z. B. Empty-State, Placeholder, Periodenlabel).
  - Kalender-Lokalisierung:
    - `CAL_LOCALE`, `CAL_MONTH_NAMES`, `CAL_DAY_SHORT`, weitere Kalender-Textkonstanten.
    - Kalendergrid und Tagesansicht nutzen diese Konstanten.
    - Recurrence-Labels und Delete-Dialogtexte lokalisiert.
    - Datumsausgabe in Kalenderbereichen auf sprachabhaengige Locale umgestellt.
  - Flashcards-JS nutzt i18n-Texte fuer Labels, Alerts, Confirm-Dialoge, Empty-States.

### Geaendert (Tab-Templates)
- `tabs/overview.php`
- `tabs/subjects.php`
- `tabs/todos.php`
- `tabs/grades.php`
- `tabs/exams.php`
- `tabs/homework.php`
- `tabs/calendar.php`
- `tabs/flashcards.php`
- `tabs/files.php`
- `tabs/admin.php`
- `tabs/admin-messages.php`

In allen oben genannten Dateien wurden statische DE-Texte auf `t('...')` umgestellt.

## Speziell heute final behobene Restpunkte
- Sidebar-Tabnamen (noch DE) -> lokalisiert.
- Hausaufgaben-Eingabefeld-Placeholder + Empty-States -> lokalisiert.
- Flashcards komplett (HTML + JS) -> lokalisiert.
- Stundenplan-Wochentage -> lokalisiert.
- Kalender komplett (Template + dynamische JS-Texte + Monats-/Wochentagsnamen + Dialogtexte) -> lokalisiert.

## Validierung
Ausgefuehrt:
- `php -l /workspaces/Informatik-Projekt/includes/i18n.php`
- `php -l /workspaces/Informatik-Projekt/current_dashboard.php`
- `php -l /workspaces/Informatik-Projekt/tabs/flashcards.php`

Ergebnis: Keine Syntaxfehler.

## Hinweis zum Repo-Status
- Es gibt auch eine geaenderte Binardatei `learnhub.db` im Worktree.
- Diese wurde nicht von der i18n-Implementierung gezielt bearbeitet, ist aber aktuell als geaendert vorhanden.

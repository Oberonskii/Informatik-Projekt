# Dokumentation: Fächer-Tab Pop-up Menüs (25.03.2026)

## Ausführung

- Task: Pop-up Menüs im Tab "Fächer" so anpassen, dass sie nicht innerhalb des Widgets angezeigt werden, sondern auf dem gesamten Bildschirm (full-screen overlay).
- Ergebnis: Modals `colorModal` und `detailsModal` aus der `.widget`-Struktur herausgezogen, als direkte Kinder von `#subjects` platziert und von `class="modal"` auf `class="modal-overlay"` mit `.modal-box` umgestellt.
- Datei geändert: `tabs/subjects.php`

## Ablauf / Details

1. In `tabs/subjects.php` wurde der Bereich mit `#colorModal` und `#detailsModal` aus dem `.widget`-Block entfernt.
2. Die Modals wurden unterhalb des `.widget`-Endes eingeschoben, damit sie auf der gesamten View (viewport) sichtbar sind.
3. Klassen gesetzt:
   - `modal-overlay` (zentrale Position, `position: fixed`, `inset:0`, dunkler Hintergrund)
   - `modal-box` (zentrales Inhaltelement)
4. Interne Funktionen bleiben unverändert (aufrufe `openColorModal()`, `closeColorModal()`, `showSubjectDetails()`, `closeDetailsModal()` funktionieren weiter)

## Benutzerprompt (original)

> Bearbeite die Pop-up Menüs im Tab "Fächer" sodass diese nicht innerhalb des widgets sondern auf dem ganzen screen angezeigt werden. Erstelle eine Dokumentationsdatei für Jonas für heute welche meinen Prompt und deine Ausgabe beinhaltet.

## Ausgabe (assistenten-seitig)

- Ein JavaScript/HTML-Update wurde erfolgreich vorgenommen: modal elements moved out of widget into full-screen overlay.
- Ein Dokumentationsfile `dokumentation/Jonas/25.03.2026_faecher_modal_update.md` wurde erstellt mit diesem Inhalt.

## Hinweise

- Keine Änderungen an funktionalen Backend-APIs oder Datenmodelle.
- visuelles Verhalten verbessert (überlagernde Modals statt innerhalb Widget Bereich).

## Nachgestellte Anforderung

### Neuer Benutzerprompt

> Passe das Pop-up menü für die Fächerdetails an damit es vom Design zu dem Rest der Website passt. Hänge meinen Prompt und deine Ausgabe der Dokumentationsdatei an.

### Zusätzliche Anpassung

- `detailsModal` wurde in `tabs/subjects.php` optisch an das bestehende Website-Design angepasst:
  - Nutzung von bestehenden globalen Klassen (`modal-overlay`, `modal-box`, `modal-header`, `modal-body`)
  - Entfernen spezialisierter inline Formatierungen zugunsten der globalen Designvorgaben
  - durchgängig eingelagerter Button-Stil (`btn-icon`) und Farbverlauf wie im Hauptbereich

### Aktuelle Ausgabe

- Anpassung wurde erfolgreich umgesetzt und in `tabs/subjects.php` gespeichert.
- Keine JavaScript-Fehler bei `get_errors`.
- Dokumentation ergänzt in `dokumentation/Jonas/25.03.2026.md`.

### Erweiterte Umsetzung für den aktuellen Prompt

#### Neuer Benutzerprompt

> Verbessere, dass das X zum schließen wieder oben rechts im Pop-up menü ist. Füge zudem hinzu, dass auch beim klick außerhalb des menüs dieses geschlossen wird. Hänge meinen Prompt und deine Ausgabe ausführlich der Dokumentationsdatei an.

#### Umsetzung im UI

- Close-Buttons (`❌` und `✕`) wurden in `tabs/subjects.php` in beiden Modalen folgendermaßen positioniert:
  - `position: absolute; top: 0.65rem; right: 0.65rem;`
  - `modal-header` auf `position: relative` gesetzt, damit absolute Positionierung referenziert auf den Headerblock.
- Die existierenden close-Klick-Buttons bleiben siehe `in Modal Header`.

#### Umsetzung bei Modaldismiss

- `current_dashboard.php` wurde erweitert um einen globalen `DOMContentLoaded`-Listener:
  - Klick auf den `detailsModal`-Overlay schließt `closeDetailsModal()`.
  - Klick auf den `colorModal`-Overlay schließt `closeColorModal()`.
- Diese Logik verwendet `event.target === detailsModal/colorModal` für exakte Overlay-Erkennung (nicht Schließen bei Klick in den Modalinhalt).

#### Ausführliche Ausgabe

- Änderungen sind in `tabs/subjects.php` und `current_dashboard.php` gemacht.
- Close-Verhalten ist jetzt visuell und funktional konsistent mit Website-Design.
- Neues Dokumentationsupdate in `dokumentation/Jonas/25.03.2026.md` (dieser Abschnitt).
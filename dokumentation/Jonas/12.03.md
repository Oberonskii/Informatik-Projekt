# Dokumentation – 12.03.2026

## Prompt (User)
Korriegiere den Stundenplan das die gespeicherten daten auch tatsächlich angezeigt werden. In der Dokumentationsdatei 11.03 findest du was gemacht wurde um zurückzuverfolgen wo der Fehler entstanden ist. Danach überarbeite den hausaufgaben tab so das man diese nicht für den tag eintragen kann sondern für die im stundenplan eingetragenen stunden und ändere die anzeige im stundenplan tab so das die hausaufgaben in der stunde angezeigt werden wie die Klassenarbeiten.

## Ausgabe (Assistant)
Ursache behoben und beide gewünschten Anpassungen umgesetzt.

1) Stundenplan speichert/lädt jetzt korrekt
- Hauptfehler war ein Spalten-Mapping-Problem in der Datenbank: Die Tabelle hatte die Reihenfolge `id, user_id, day, time, subject, period, room`, der Code hat aber ohne Spaltennamen in anderer Reihenfolge geschrieben. Dadurch wurden Stunde/Zeit/Fach vertauscht gespeichert.
- Fix in `backend.py`: Inserts und Updates nutzen explizite Spaltenlisten.
- Fix in `backend.py`: Laden filtert nur gültige Einträge mit gesetzter `period`.
- Migration in `backend.py`: Bereits falsch gespeicherte Einträge werden beim Backend-Start automatisch erkannt und korrigiert.

2) Hausaufgaben auf Stunden (statt Tag) umgebaut
- Datenmodell im Frontend ist jetzt: `day -> period -> Liste von Aufgaben`.
- Umbau + Legacy-Migration in `current_dashboard.php`: Alte Tages-Einträge werden in die erste vorhandene Stunde des Tages übernommen.
- Hausaufgaben können nur noch bei im Stundenplan vorhandenen Stunden eingetragen werden.
- Funktionen für Hinzufügen/Löschen arbeiten jetzt mit `day + period`.

3) Hausaufgaben im Stundenplan pro Stunde anzeigen (wie Klassenarbeiten)
- Die Hervorhebung/Badges laufen jetzt periodengenau statt tagesweit.

Validierung
- `php -l current_dashboard.php`: keine Syntaxfehler
- `python3 -m py_compile backend.py`: erfolgreich
- Fehlercheck im Editor für `backend.py` und `current_dashboard.php`: keine Fehler

## Geänderte Dateien und Inhalte

1) `backend.py`
- Import ergänzt: `re`
- In `init_db()` Datenreparatur ergänzt für fehlerhaft gespeicherte Stundenplan-Zeilen:
  - erkennt alte Fehlbelegung (`time <- period`, `subject <- time`, `period <- subject`)
  - korrigiert auf die richtige Belegung
- `GET /timetable/{user_id}` angepasst:
  - nur Einträge mit gesetzter `period` werden geliefert
- `POST /timetable/{user_id}` angepasst:
  - `INSERT` nutzt jetzt explizite Spaltenliste
- `PUT /timetable/{user_id}/{entry_id}` angepasst:
  - `UPDATE`-Zuordnung auf korrekte Feldreihenfolge
- `POST /timetable/{user_id}/bulk` angepasst:
  - `INSERT` nutzt explizite Spaltenliste

2) `current_dashboard.php`
- Nach `loadTimetable()` zusätzlich:
  - `normalizeHomeworkData()`
  - Re-Render von Homework im Homework-Tab und Timetable-Tab
- Neue Hilfsfunktionen ergänzt:
  - `getScheduledPeriodsForDay(day)`
  - `normalizeHomeworkData()`
- `homework_data`-Format auf periodengenau umgestellt:
  - alt: `{ day: ["...", "..."] }`
  - neu: `{ day: { period: ["...", "..."] } }`
- `getHomeworkHighlights()` umgestellt auf Key-Format `day-period`
- `renderTimetableView()` angepasst:
  - Hausaufgaben-Badges werden nun pro Stunde angezeigt
- `saveTimetable()` erweitert:
  - nach Speichern `normalizeHomeworkData()` zum Bereinigen/Zuordnen
- `renderHomework()` komplett auf stundenbasierte Eingabe umgestellt:
  - pro eingetragener Stunde eigene Eingabe-/Listenzeile
  - keine Eingabe mehr „nur pro Tag“
- `addHomework(day, period)` und `deleteHomework(day, period, index)` angepasst
- `renderOverviewHomeworks()` angepasst:
  - sortiert nach Wochentag und Stunde
  - zeigt „Tag · X. Stunde“

## Hinweis
- Zusätzlich wurde versehentlich geänderte Bytecode-Datei `__pycache__/backend.cpython-312.pyc` wieder zurückgesetzt, damit nur Quellcode-Änderungen im Stand bleiben.
